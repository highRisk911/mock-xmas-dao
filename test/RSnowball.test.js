

const { expect } = require('chai');

const { BN, expectEvent, expectRevert, constants } = require('@openzeppelin/test-helpers')

const RSnowball = artifacts.require('RSnowball');

contract('RSnowball', function ([creator, other]) {
    const NAME = 'Santa';
    const SYMBOL = 'HOHOHO';
}
)